<h1>Welcome to BarTinder</h1>
<% if !session[:user_id] %>
    <h2>Please <%= link_to "Log In", login_path %> or <%= link_to "Create an Account", new_user_path %> to continue</h2>
<% end %>

<div style="margin-bottom: 200 px", class="flex-container">
    <div class="column">
        Most cocktails made: <%= link_to User.most_cocktails.name, user_path(User.most_cocktails) %> - <%= User.most_cocktails.cocktails.count%> cocktails
    </div>
    
    <div class="column">
        User giving out the most love: <%= link_to User.most_likes_given.name, user_path(User.most_likes_given) %> - <%= User.most_likes_given.likes.count%> likes
    </div>
    
    <div class="column">
        Chattiest user: <%= link_to User.most_chatty.name, user_path(User.most_chatty) %> - <%= User.most_chatty.comments.count %> comments given
    </div>      
</div>


<div id="bottom", class="flex-container">

    <div class="column">
        User being shown the most love: <br><%= link_to User.most_liked.name, user_path(User.most_liked) %> has <%= User.most_liked.liked %> likes on their cocktails
    </div>
        
    <div id="bot-mid", class="column">
        Most discussed user: <%= link_to User.most_commented.name, user_path(User.most_commented) %> has <br><%= User.most_commented.commented %> comments on their cocktails
    </div>
        
    <div id="bot-right", class="column">
        <%= link_to "Random User", user_path(User.all.sample) %><br>
    </div>

</div>